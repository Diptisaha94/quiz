import React from 'react';
import Header from './Header';

const Blog = () => {
    return (
        <div>
            <Header></Header>
            this is blog page
        </div>
    );
};

export default Blog;